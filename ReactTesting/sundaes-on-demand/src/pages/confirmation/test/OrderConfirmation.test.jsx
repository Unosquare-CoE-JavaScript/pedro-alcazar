import { render, screen, act } from "../../../test-utils/testing-library-utils";
import OrderConfirmation from "../OrderConfirmation";

test('Test Order Confirmation', async() => {
    // render(<OrderConfirmation />)

    // act(async() => {
    //     const loadingText = screen.getByText('Loading..');
    //     expect(loadingText).toBeInTheDocument();
    //     const noLoadingText = await screen.findByText('Loading..');
    //     expect(noLoadingText).not.toBeInTheDocument();
    // })


    // I need check the process for get the next state of component
    
    // const thankYouHeader = await screen.findByRole('heading', { name : 'Thank you' })
    // expect(thankYouHeader).toBeInTheDocument()
})